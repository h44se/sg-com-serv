[project]
name = "f1dashboard-backend"
version = "0.1.0"
description = "Formula One dashboard backend and aggregation service"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=9,<10"]

[tool.pytest.ini_options]
pythonpath = ["src"]
