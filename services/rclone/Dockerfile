FROM rclone/rclone:latest
# Official rclone Alpine image — no changes needed.
# The service is invoked with `docker compose run --rm rclone <args>`,
# and root (user: 0:0 in compose) gives full read access to every volume.
ENTRYPOINT ["rclone"]
